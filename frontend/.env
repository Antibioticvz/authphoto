# API Configuration
# For local desktop development, use localhost
# For mobile testing on the same network, use your computer's IP address
# NOTE: Backend runs on HTTP, frontend needs HTTPS for camera access
VITE_API_BASE_URL=http://192.168.100.4:3000
