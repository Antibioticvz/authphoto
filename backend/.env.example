# Application
NODE_ENV=development
PORT=3000
API_PREFIX=api/v1

# Logging
LOG_LEVEL=debug

# Cache
CACHE_TTL=30

# HTTPS (auto-detects backend/.cert when not set)
# HTTPS_ENABLED=true
# HTTPS_CERT_PATH=./.cert/cert.pem
# HTTPS_KEY_PATH=./.cert/key.pem

# CORS
CORS_ORIGIN=http://localhost:5173
